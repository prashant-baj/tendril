[pytest]
# Discover tests across the monorepo; skip build/vendor dirs.
testpaths =
    tests
    infra/tests
    agents/hello_agent/tests
    app/api/tests
    app/orchestrator/tests
norecursedirs = cdk.out node_modules .venv .git dist .angular
addopts = -q
